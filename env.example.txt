# FieldOps - Environment Variables Örnek Dosyası
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun
# .env dosyasını GIT'E EKLEMEYİN! (.gitignore'da olmalı)

# ============================================================================
# GÜVENLİK
# ============================================================================
# Django Secret Key - Mutlaka değiştirin!
# Üretmek için: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-change-me-in-production

# Debug Modu - Production'da mutlaka False olmalı!
DEBUG=True

# İzin verilen host'lar (virgülle ayırın)
# Production'da: yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=*

# ============================================================================
# VERİTABANI
# ============================================================================
# Development: Boş bırakın (SQLite kullanılır)
# Production: PostgreSQL connection string
# Örnek: postgresql://user:password@localhost:5432/fieldops_db
DATABASE_URL=

# ============================================================================
# EMAIL AYARLARI
# ============================================================================
# Email backend (development: console, production: smtp)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# SMTP Ayarları (Production için)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@fieldops.com

# ============================================================================
# DIĞER AYARLAR
# ============================================================================
# Redis URL (Cache ve Celery için - opsiyonel)
# REDIS_URL=redis://localhost:6379/0

# Sentry DSN (Hata takibi için - opsiyonel)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id




